{
  "name": "httpkit",
  "module": "index.ts",
  "type": "module",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "valibot": "^1.1.0",
    "zod": "^4.0.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build src/index.ts --outdir=dist --target=browser --format=esm",
    "dev": "bun test --watch"
  },
  "dependencies": {}
}
